/**
 * @file Procedural Terrain Generator
 * @author James Timotiwu <jit2@illinois.edu>
*/

/* 1. Write procedural generation */
/* Create flat triangulated surface */
/* Produce triangles */
/* Generate tessellated quadrilateral / divide rectangles up into triangles */
/* Generate tessellated quad using recursion / iteration */
/**
* Generate tessellated quad using recursion
* @param {*} n   * @param {*} min_x 
* @param {*} max_x   * @param {*} min_y 
* @param {*} max_y   * @param {*} vertex_array 
*/
function generatePlaneFromSubdivision(n, min_x, max_x, min_y, max_y, vertex_array) {
    return 0;
}
/**
 * Subdivide a triangle
 * @param {*} a 
 * @param {*} b 
 * @param {*} c 
 * @param {*} num_sub_divs 
 * @param {*} vertex_array 
 */
function divideTriangle(a, b, c, num_sub_divs, vertex_array) {
     return 0;
}

function getNormal() {
    return 0;
}

function generateTerrain() {
    return 0;
}

/**
 * Lab 3
 */
function getVertex() {
    return 0;
}

function setVertex(x, y, vertex_array) {
    return 0;
}

/**
 * Fills javascript arrays containing the vertex positions
 * Triangle connectivity
 * Vertex normals
 */
function generateTriangles() {
    return 0;
}
